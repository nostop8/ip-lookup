name: React App CI

on:
  push:
    paths:
      - 'packages/react-app/**'
      - 'packages/shared/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/react-app-ci.yml'
  pull_request:
    paths:
      - 'packages/react-app/**'
      - 'packages/shared/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/react-app-ci.yml'

jobs:
  react-app-ci:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [18.x, 20.x]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run type-check --workspace=packages/react-app

      - name: Lint
        run: npm run lint --workspace=packages/react-app

      - name: Run tests
        run: npm run test:ci --workspace=packages/react-app

      - name: Build
        run: npm run build --workspace=packages/react-app

      - name: Upload coverage reports
        if: matrix.node-version == '20.x'
        uses: codecov/codecov-action@v3
        with:
          file: ./packages/react-app/coverage/lcov.info
          flags: react-app
          name: react-app-coverage